import React from 'react';

/**
 * A functional React component that renders a button with the text 'hellouserappunik'.
 * 
 * @returns {JSX.Element} A button element with the specified text.
 */
const HellouserappunikButton = () => {
  return (
    <button type="button" className="hellouserappunik-button">
      hellouserappunik
    </button>
  );
};

export default HellouserappunikButton;